# Copy this file to grafana-secret.values.yaml, replace the password, and do
# not commit the resulting file.
grafana:
  admin:
    existingSecret: grafana-admin-credentials
    userKey: admin-user
    passwordKey: admin-password

grafanaCredentials:
  create: true
  username: "admin"
  password: "admin"
